<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AS1">
        <activity
            android:name="screens.ChatScreen"
            android:exported="true" />
        <activity
            android:name="screens.FriendInteractions"
            android:exported="true" />
        <activity
            android:name="screens.FriendProfileScreen"
            android:exported="true"/>
        <activity
            android:name="screens.ProfileSettings"
            android:exported="true" />
        <activity
            android:name="screens.ProfileScreen"
            android:exported="true" />
        <activity
            android:name="screens.MusicSwipe"
            android:exported="true" />
        <activity
            android:name="screens.SeedSetter"
            android:exported="true" />
        <activity
            android:name="screens.LoginScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
